{
  "name": "hydrostatic-force-surfaces",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive CE2134 learning platform for hydrostatic forces on plane and curved surfaces",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}

